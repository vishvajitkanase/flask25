apiVersion: v1
kind: Pod
metadata:
  name: flask-app-pod
  labels:
    app: flask-app
spec:
  containers:
    - name: flask-container
      image: vishvajitkanase/flask25:latest
      ports:
        - containerPort: 5000
      env:
        - name: FLASK_ENV
          value: "production"
